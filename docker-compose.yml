version: '3.8'

services:
  authsystem:
    build: .
    volumes:
      - ./data:/data
    environment:
      - DB_PATH=/data/auth.db
      - PORT=5000
    ports:
      - "5000:5000"
    restart: unless-stopped
